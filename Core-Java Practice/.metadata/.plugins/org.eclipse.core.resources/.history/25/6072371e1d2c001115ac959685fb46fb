package com.day3;

import java.util.Scanner;

public class Test2dArray {
	public static void main(String[] args) {
		// int arr[][]=new int[3][3];
		int arr[][] = { { 11, 12, 13 }, { 21, 22, 23 }, { 31, 32, 33 } };
		// Array2DService.accpectData(arr);
		//Array2DService.displayData(arr);
	   
		  int choice=0;
	     Scanner sc= new Scanner(System.in);
	 
	     do {
	    	  
	    	 System.out.println(" 0:Display the Elements\n 1:Find Maxium number\n 2: Find Minimum Number \n 3: Find the Sum of element\n 4:Finf the Rowwise sum\n 5:Find ColoumwiseSum");
	    	System.out.println("6:FindRowWiseMax\n 7:Find Coloumwisemax\n 8:FindRowwiseMin\n 9:Find Coloumnwisemin\n 10:Exit");
	    	System.out.print("choice");
	    	sc.nextInt();
	    	 switch (choice) {
			case 1->{
				Array2DService.displayData(arr);
			}
			case 2->{
				int max=Array2DService.findMax(arr);
				System.out.println("Maximun number is "+max);
			}
			case 3->{
				int min =Array2DService.findMin(arr);
				System.out.println("Minimun number is "+min);
			}
			case 4->{
				int sum=Array2DService.findSum(arr);
				System.out.println("Sum of all elements from array "+sum);
			}
			case 5->{
				int []rowwisesum=Array2DService.findRowiseSum(arr);
			     for(int i=0;i<rowwisesum.length;i++) {
			    	 System.out.println("Sum of "+i +" row is = "+rowwisesum[i]);
			     }
			}
			case 6->{
				int [] coloumnwisesum=Array2DService.findColumnwiseSum(arr);
			     for(int i=0;i<coloumnwisesum.length;i++) {
			    	 System.out.println("Sum of "+i +" Coloumn is = "+coloumnwisesum[i]);
			     }
			}
			case 7->{
			     
			     int [] rowwisemax =Array2DService.findRowwiseMax(arr);
			     for(int i=0;i<rowwisemax.length;i++) {
			    	 System.out.println("Rowwise max number of "+i+" row is = "+rowwisemax[i]);
			     }
			}
			case 8->{
			     int [] coloumnwisemax =Array2DService.findColoumwiseMax(arr);
			     for(int i=0;i<coloumnwisemax.length;i++) {
			    	 System.out.println("columwise max number of "+i+" coloumn is = "+coloumnwisemax[i]);
			     }
			
			}
			case 9->{

			     int rowwisemin[]= Array2DService.findRowwisemin(arr);
				     for(int i=0;i<rowwisemin.length;i++) {
				    	 System.out.println("Rowwise min number of "+i+" row is = "+rowwisemin[i]);
				     }
			}
			case 10->{
				  int coloumwisemin[]=Array2DService.findColoumnwiseMin(arr);
				     for(int i=0;i<coloumwisemin.length;i++) {
				    	 System.out.println("Columnwisw min number of "+i+" coloun is = "+coloumwisemin[i]);
				     }
				     
			}
				
			
			case 11->{
				System.out.println("Thank your For Visiting");
			}
	    	 	
			
			default->{
			throw new IllegalArgumentException("Unexpected value: " + choice);	}
	    	 
	    	 }
	    	 
			
		} while (choice!=11);

	     
	}};
	     
	     
	     
	     
	     
	     
	     
	     
	     
	     
	     
	     
	    
